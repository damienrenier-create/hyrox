[
  {
    "id": "column.public.session.opensAt",
    "label": "Add column \"opensAt\" to \"session\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "opensAt",
        "table": "session"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"opensAt\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "opensAt"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"opensAt\"",
        "sql": "ALTER TABLE \"public\".\"session\" ADD COLUMN \"opensAt\" timestamptz"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"opensAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "opensAt"
        ]
      }
    ]
  }
]