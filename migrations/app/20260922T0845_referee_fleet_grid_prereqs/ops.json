[
  {
    "id": "column.public.session.refereeMode",
    "label": "Add column \"refereeMode\" to \"session\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "refereeMode",
        "table": "session"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"refereeMode\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "refereeMode"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"refereeMode\"",
        "sql": "ALTER TABLE \"public\".\"session\" ADD COLUMN \"refereeMode\" bool DEFAULT false NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"refereeMode\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "session",
          "refereeMode"
        ]
      }
    ]
  },
  {
    "id": "column.public.team.order",
    "label": "Add column \"order\" to \"team\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "order",
        "table": "team"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"order\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "team",
          "order"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"order\"",
        "sql": "ALTER TABLE \"public\".\"team\" ADD COLUMN \"order\" int4"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"order\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "team",
          "order"
        ]
      }
    ]
  }
]